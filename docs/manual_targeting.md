# Manual Target Acquisition

## Implemented workflow

```text
PDF upload
→ lazy slide rendering
→ embedded text extraction or OCR fallback
→ AOI generation
→ manual rectangle drawing
→ normalized bbox
→ AOI overlap ranking
→ selected context preview
→ typed command capture
